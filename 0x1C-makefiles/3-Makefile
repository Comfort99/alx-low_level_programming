CC = gcc
SRC = main.c school.c
OBJ = ${SRC:%.c=%.o}
NAME = school
RM = rm -f

%.o: %.c m.h
	${CC} -c -o $@ $<

all: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

clean:
	${RM} *.~ ${NAME}

oclean:
	${RM} ${OBJECT}

fclean: clean oclean

re: fclean all

%.o: %.c m.h
	${CC} -C $< -O $@
